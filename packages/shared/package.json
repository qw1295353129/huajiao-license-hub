{
  "name": "@license-hub/shared",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test \"dist-test/**/*.spec.js\""
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "@types/node": "^22.10.0"
  }
}
